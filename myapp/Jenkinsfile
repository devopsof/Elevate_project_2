pipeline {

    agent {label "First"}

    stages {
        stage ("Git clone") {
            steps {
                git url: "https://github.com/devopsof/Elevate_project_2.git", branch: "main"
            }
        }

        stage ("Docker image") {
            steps {
                sh " docker build -t irady/myapp myapp/. "
            }
        }

        stage ("Dockerhub push") {
            steps {
                sh "docker push irady/myapp:latest"
            }
        }

        stage ("Run image") {
            steps {
                sh "docker start -p 8888:8888 irady/myapp:latest"
            }
        }
    }
}